// Moved to spikes/collab-shared/src/contracts.ts (work package 3b: the
// candidate-agnostic contract lives in one place; this file only re-exports
// it so existing imports in this candidate keep working).
export type { CandidateName, EngineAdapter, EngineDiff, Frontier } from "@sylvode/collab-shared";
